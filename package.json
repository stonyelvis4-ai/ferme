{
  "name": "ferm-plus",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "typescript-eslint": "^8.18.2"
  },
  "scripts": {
    "build": "corepack pnpm -r build",
    "dev": "corepack pnpm --parallel --filter @ferm-plus/backend --filter @ferm-plus/frontend dev",
    "lint": "corepack pnpm -r lint",
    "test": "corepack pnpm -r test"
  }
}
